import 'package:flutter/material.dart';

class RefrigeratorContents extends StatelessWidget{
  const RefrigeratorContents ({Key? key}) :super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
